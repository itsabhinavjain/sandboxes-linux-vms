# Not used by the default NAT + Tailscale networking mode (see PLAN.md).
# Left as a placeholder for a future bridged-networking mode. If/when that
# mode is built, 00_init.sh would render this and pass it to cloud-localds
# via `--network-config`.
#
# version: 2
# ethernets:
#   enp1s0:
#     dhcp4: true
